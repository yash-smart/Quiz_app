<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Question</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <ul class="nav nav-pills">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/main/<%= owner %>">Home</a>
        </li>
        <% if (locals.update) { %>
          <li class="nav-item">
            <a class="nav-link" href="/question/<%= quest_id %>">Back</a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link" href="/quiz/<%= quiz_id %>">Back</a>
          </li>
        <% } %>
    </ul>
    <% if (locals.update) { %>
      <form action="/update_quest/<%= quest_id %>" method="POST">
      <textarea name="question_text" rows="4" cols="50"><%= question_data.question_text %></textarea>
      <input type="number" name="marks" placeholder="marks" value="<%= question_data.marks %>">
      <input type="number" name="min_marks" placeholder="Minimum marks" value="<%= question_data.min_marks %>">
    <% } else {%>
      <form action="/question_submit/<%= quiz_id %>" method="POST">
      <textarea name="question_text" rows="4" cols="50"></textarea>
      <input type="number" name="marks" placeholder="marks">
      <input type="number" name="min_marks" placeholder="Minimum marks">
    <% } %>
        <% if (locals.update) { %>
          <input type="submit" value="Update question">
        <% }else { %>
          <input type="submit" value="Add question">
        <% } %>
    </form>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>